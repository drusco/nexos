name: unit-tests

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".node-version"

      - name: Cache npm cache directory
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Determine target branch
        id: target
        run: echo "branch=${{ github.event_name == 'pull_request' && github.base_ref || github.ref_name }}" >> "$GITHUB_OUTPUT"

      - name: Run production tests
        if: steps.target.outputs.branch == 'main'
        env:
          COVERAGE_THRESHOLD: '{"global": {"branches": 100, "functions": 100, "lines": 100, "statements": 100}}'
        run: npx jest --coverage --coverageThreshold="$COVERAGE_THRESHOLD"

      - name: Run development tests
        if: steps.target.outputs.branch == 'next'
        run: npm run test-coverage

      - name: Upload results to Codecov
        if: github.event_name == 'push' && contains('main next', github.ref_name)
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage/lcov.info
